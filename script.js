const SUPABASE_URL="https://qyipadinsphoyxotrceo.supabase.co";
const SUPABASE_KEY="sb_publishable_S73dZKZ9ro03lWDbHFzZhw_5t5pDtGt";
const db=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);
const esc=v=>String(v??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#039;");
const date=v=>v?new Date(v).toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"}).toUpperCase():"";
const img=(a,cls="story-image")=>a.image_url?`<img class="${cls}" src="${esc(a.image_url)}" alt="${esc(a.title)}" loading="lazy">`:`<div class="${cls.includes("media")?"media-img":"story-placeholder"}">${esc(a.category).toUpperCase()}</div>`;
const card=a=>`<article class="story-card reveal"><a href="article.html?slug=${encodeURIComponent(a.slug)}">${img(a)}<div class="story-body"><span class="category-pill">${esc(a.category).toUpperCase()}</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"Read the latest story from Paw Prints Weekly.")}</p><div class="meta"><span>${esc((a.author||"PPW Staff").toUpperCase())}</span><span>${date(a.published_at)}</span></div></div></a></article>`;
function setupNav(){const toggle=document.getElementById("menuToggle"),nav=document.getElementById("mainNav");toggle?.addEventListener("click",()=>{const open=nav.classList.toggle("open");toggle.classList.toggle("active",open);toggle.setAttribute("aria-expanded",String(open));document.body.classList.toggle("menu-open",open)});nav?.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");toggle?.classList.remove("active");toggle?.setAttribute("aria-expanded","false");document.body.classList.remove("menu-open")}));const current=document.getElementById("currentDate");if(current)current.textContent=new Date().toLocaleDateString("en-US",{month:"long",year:"numeric"}).toUpperCase()}
function revealSetup(){const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.remove("visible");void e.target.offsetWidth;e.target.classList.add("visible")}else e.target.classList.remove("visible")}),{threshold:.08});document.querySelectorAll(".reveal").forEach(e=>observer.observe(e))}
function sectionRender(category,items){const id={"School News":"cat-school-news","Student Life":"cat-student-life","Sports":"cat-sports","Perspectives":"cat-perspectives","World Events":"cat-world-events","Features":"cat-features","Entertainment & Media":"cat-entertainment-media","Editorial":"cat-editorial"}[category];const el=document.getElementById(id);if(!el)return;if(!items.length){el.innerHTML=`<div class="empty-card">No ${esc(category.toLowerCase())} stories have been published yet.</div>`;return}if(category==="School News"){el.innerHTML=`<div class="school-list">${items.map((a,i)=>`<a class="school-row reveal" href="article.html?slug=${encodeURIComponent(a.slug)}"><span class="num">${String(i+1).padStart(2,"0")}</span><div><span class="category-pill">${esc(a.category).toUpperCase()}</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p></div>${a.image_url?`<img src="${esc(a.image_url)}" alt="${esc(a.title)}" loading="lazy">`:""}</a>`).join("")}</div>`}else if(category==="Student Life"){el.innerHTML=`<div class="life-grid">${items.slice(0,3).map(a=>`<a class="life-card reveal" href="article.html?slug=${encodeURIComponent(a.slug)}">${a.image_url?`<img src="${esc(a.image_url)}" alt="" loading="lazy">`:""}<div><span class="category-pill">${esc(a.category).toUpperCase()}</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p></div></a>`).join("")}</div>`}else if(category==="Sports"){el.innerHTML=`<div class="sports-grid">${items.map(a=>`<a class="sports-card reveal" href="article.html?slug=${encodeURIComponent(a.slug)}">${a.image_url?`<img class="sports-thumb" src="${esc(a.image_url)}" alt="" loading="lazy">`:`<div class="sports-thumb"></div>`}<div><span class="category-pill">SPORTS</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p></div><span class="sports-arrow">→</span></a>`).join("")}</div>`}else if(category==="Perspectives"){el.innerHTML=`<div class="perspective-grid">${items.map(a=>`<a class="perspective-card reveal" href="article.html?slug=${encodeURIComponent(a.slug)}"><span class="category-pill">PERSPECTIVE</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p><div class="meta"><span>${esc((a.author||"PPW Staff").toUpperCase())}</span><span>${date(a.published_at)}</span></div></a>`).join("")}</div>`}else if(category==="World Events"){el.innerHTML=`<div class="world-grid">${items.map(a=>`<a class="world-card reveal" href="article.html?slug=${encodeURIComponent(a.slug)}">${a.image_url?`<img src="${esc(a.image_url)}" alt="" loading="lazy">`:""}<span class="category-pill">WORLD EVENTS</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p><div class="meta"><span>${esc((a.author||"PPW Staff").toUpperCase())}</span><span>${date(a.published_at)}</span></div></a>`).join("")}</div>`}else if(category==="Features"){el.innerHTML=`<div class="features-grid">${items.map(a=>`<a class="feature-card reveal" href="article.html?slug=${encodeURIComponent(a.slug)}">${a.image_url?`<img src="${esc(a.image_url)}" alt="" loading="lazy">`:`<div class="feature-img"></div>`}<div class="story-body"><span class="category-pill">FEATURE</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p><div class="meta"><span>${esc((a.author||"PPW Staff").toUpperCase())}</span><span>${date(a.published_at)}</span></div></div></a>`).join("")}</div>`}else if(category==="Entertainment & Media"){el.innerHTML=`<div class="media-grid">${items.map(a=>`<a class="media-card reveal" href="article.html?slug=${encodeURIComponent(a.slug)}">${a.image_url?`<img class="media-img" src="${esc(a.image_url)}" alt="" loading="lazy">`:`<div class="media-img"></div>`}<div class="story-body"><span class="category-pill">ENTERTAINMENT &amp; MEDIA</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p></div></a>`).join("")}</div>`}else{el.innerHTML=`<div class="editorial-grid">${items.map(a=>`<a class="editorial-card reveal" href="article.html?slug=${encodeURIComponent(a.slug)}"><span class="category-pill">EDITORIAL</span><h3>${esc(a.title)}</h3><p>${esc(a.excerpt||"")}</p><div class="meta"><span>${esc((a.author||"PPW Staff").toUpperCase())}</span><span>${date(a.published_at)}</span></div></a>`).join("")}</div>`}}
async function loadArticles(){const {data,error}=await db.from("articles").select("id,title,slug,excerpt,content,category,author,image_url,published_at,featured").eq("status","published").order("published_at",{ascending:false});if(error){document.getElementById("latestArticles").innerHTML='<div class="empty-card">The newsroom is temporarily unavailable.</div>';return}const articles=data||[];const featured=articles.find(a=>a.featured)||articles[0];const featureEl=document.getElementById("featuredArticle");if(featured){featureEl.innerHTML=`<article class="featured-card reveal"><a href="article.html?slug=${encodeURIComponent(featured.slug)}">${img(featured)}<div class="story-body"><div class="featured-badge">EDITOR'S LEAD STORY</div><span class="category-pill">${esc(featured.category).toUpperCase()}</span><h3>${esc(featured.title)}</h3><p>${esc(featured.excerpt||"Read the lead story from Paw Prints Weekly.")}</p><div class="meta"><span>${esc((featured.author||"PPW Staff").toUpperCase())}</span><span>${date(featured.published_at)}</span></div></div></a></article>`}else{featureEl.innerHTML='<div class="empty-card">Your first featured story will appear here after you publish it.</div>'}const latest=articles.slice(0,6);document.getElementById("latestArticles").innerHTML=latest.length?latest.map(card).join(""):'<div class="empty-card">No published stories yet.</div>';["School News","Student Life","Sports","Perspectives","World Events","Features","Entertainment & Media","Editorial"].forEach(c=>sectionRender(c,articles.filter(a=>a.category===c).slice(0,6)));revealSetup()}
setupNav();setTimeout(()=>{const loader=document.getElementById("ppwLoader");loader?.classList.add("is-hidden");document.body.classList.remove("ppw-loading");document.documentElement.classList.remove("ppw-loader-active");setTimeout(()=>loader?.remove(),800)},1800);loadArticles();
